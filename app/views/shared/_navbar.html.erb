<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <%= link_to "OrangeBank", root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Debug", debug_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "New transaction", new_transaction_path, class: "nav-link" %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">

          <% if usr_signed_in? %>
            <%= current_usr.email %>
          <% else %>
              Not signed in
            <% end %>
          </a>
          <ul class="dropdown-menu">
            <% if usr_signed_in? %>
              <li><p class="dropdown-item"><%= current_usr.email %></p></li>
              <li><%= link_to "My accounts", accounts_path, class: "dropdown-item" %></li>
              <li><%= button_to "Log out", destroy_usr_session_path, class: "dropdown-item", method: :delete %></li>
            <% else %>
              <li><%= link_to "Log in", new_usr_session_path, class: "dropdown-item" %></li>
              <li><%= link_to "Sing up", new_usr_registration_path, class: "dropdown-item" %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>

<% if notice %>
<div class="alert alert-info" role="alert">
    <%= notice %>
</div>
<% end %>

<% if alert %>
<div class="alert alert-danger" role="alert">
    <%= alert %>
</div>
<% end %>